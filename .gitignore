# 敏感配置: 含 Tushare token, 切勿提交
tushare_mcp.json

# 生成的报告(每次运行重新产出)
report_*.html
report_*.xlsx

# push.py 发布时本地克隆的 Pages 仓库工作副本
.pages_publish/

# 定时任务(launchd)运行日志
scan.log

# 历史记录数据(个人持仓/快照, 每次运行变动; 代码 history.py 仍纳入版本控制)
history/

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store
